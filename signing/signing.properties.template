# Copia este archivo a `signing.properties` en la raíz del proyecto y rellena los valores.
# No subas `signing.properties` a repositorios públicos.

storeFile=path\\to\\my-release-key.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
